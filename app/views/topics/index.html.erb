<p id="notice"><%= notice %></p>

<h1>なまおもの掲示板へようこそ</h1>

<table>
  <thead>
    <tr>
      <th>投稿一覧</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= topic.title %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規投稿', new_topic_path %>
